<ul>
    {% for profile in profiles %}
        <li>
            <a
                class="text-blue-600 visited:text-purple-600"
                href="xhprof/{{ profile.name }}"
                download
            >{{ profile.name }}</a>
            <span>{{ profile.time|date }}</span>
        </li>
    {% endfor %}
</ul>
